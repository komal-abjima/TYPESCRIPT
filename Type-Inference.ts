//declare a variable message and initialize it with the value "Hello Typescript" Infer the type message using type inference

let message = "Hello Typescript";
console.log(message);

//wa function calculateArea that takes the length and width of the parameters as numbers and return their product of the return type of the function using type interference.
function calculateArea(l:number, b:number){

    return l * b;

}
console.log(calculateArea(2,3));